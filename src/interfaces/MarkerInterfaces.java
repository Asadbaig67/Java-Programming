package interfaces;

interface MarkerInterface {}

public class MarkerInterfaces implements MarkerInterface {


    // 1. Marker Interface is empty interface
    // 2. Used to mark class so that it can be identified
    // 3. Serializable is marker interface in java

    // It tells the Java Object Serialization API that an object can be converted to a byte stream — so it can be:
    // --> Saved to a file
    // --> Sent over a network
    // --> Stored in a database



}
